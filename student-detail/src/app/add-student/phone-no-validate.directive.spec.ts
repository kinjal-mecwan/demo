import { PhoneNoValidateDirective } from './phone-no-validate.directive';

describe('PhoneNoValidateDirective', () => {
  it('should create an instance', () => {
    const directive =new PhoneNoValidateDirective()
    expect(directive).toBeTruthy();
  });
});
